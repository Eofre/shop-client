import { FC } from "react";
import classes from "./LoginPage.module.scss";

interface LoginPageProps {}

export const LoginPage: FC<LoginPageProps> = () => {
  
  return <section className={classes.loginPage}></section>
};
